version: 2
jobs:
  build:
    docker:
      - image: hashicorp/packer:light

    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: install dependencies
          command: |
            packer push -name=rustic/fedora29-minimal fedora29_srv.json