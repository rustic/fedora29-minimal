version: 2
jobs:
  build:
    docker:
      - image: hashicorp/packer:light

    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: install dependencies
          command: |
            packer -v